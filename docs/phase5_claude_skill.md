## Phase 5 Claude Skill

## Purpose

Phase 5 documents and implements the Claude-side workflow that uses the
HealthEx MCP server for immunization-gap analysis.

## Focus Areas

- confirm the exact MCP flow needed for the current user record
- define the context the skill needs as input
- document the prompt or packaging shape that is easiest for reviewers to follow

## Constraints

- keep the skill grounded in the real patient data that the repo can actually access
- document any mismatch between the MCP path and the browser-token FHIR path
